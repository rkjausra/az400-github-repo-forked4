name: Reusable Tests

on:
  workflow_call:

jobs:
  tests:
    name: Tests Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Running tests..."
          echo "Tests passed"
